management:
  metrics:
    export:
     prometheus:
      enabled: true

  endpoints:
    web:
      exposure:
        include: prometheus, health

spring:
  liquibase:
    enabled: true
    change-log: classpath:/dbchangelog/db.changelog-master.xml

  jpa:
    hibernate:
      ddl-auto: none